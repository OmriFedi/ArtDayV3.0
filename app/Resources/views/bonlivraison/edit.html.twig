{% include 'AdminHeader.html.twig' %}

    {% block mainPanel %}

<div class="mainpanel">

    <div class="contentpanel">
    <h1>Bonlivraison edit</h1>

    {{ form_start(edit_form) }}

        <div class="form-group">
            <label class="info-title" for="exampleInputEmail1">Bon  de Livraison<span>*</span></label>
            {{ form_widget(edit_form.num_bl, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="form-group">
            <label class="info-title" for="exampleInputEmail1">Date Bon  de Livraison<span>*</span></label>
            {{ form_widget(edit_form.date_l) }}
        </div>

        <div class="form-group">
            <input type="submit" value="Edit" class="btn btn-success"/>
        </div>



    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('bonlivraison_index') }}" class="btn btn-default">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}
        </li>
    </ul>
    </div>
</div><!-- mainpanel -->
    {% endblock %}
<script type="text/javascript">
    $( "#livraison" ).toggleClass( "nav-parent active" )
</script>

{% include 'AdminFooter.html.twig' %}
