{% include 'AdminHeader.html.twig' %}

{% block body %}
    {% block mainPanel %}

        <div class="mainpanel">

            <div class="contentpanel">
    <h1>Produitenpromotion</h1>
    <div class="table-responsive">
    <table class="table table-bordered table-inverse table-striped nomargin">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produitEnPromotion.id }}</td>
            </tr>
        </tbody>
    </table>
    </div>
    <ul>
        <li>
            <a href="{{ path('produitenpromotion_index') }}" class="btn btn-default">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('produitenpromotion_edit', { 'id': produitEnPromotion.id }) }}" class="btn btn-success">Edit</a>

            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}
        </li>
    </ul>

    </div>
    </div><!-- mainpanel -->
{% endblock %}



{% endblock %}
<script type="text/javascript">
    $( "#promotion" ).toggleClass( "nav-parent active" )
</script>
{% include 'AdminFooter.html.twig' %}
