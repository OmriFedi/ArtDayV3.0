{% include 'AdminHeader.html.twig' %}

{% block body %}
    {% block mainPanel %}

<div class="mainpanel">

    <div class="contentpanel">
    <h1>Produitenpromotion creation</h1>

    {{ form_start(form) }}
        <div class="form-group">
            <label class="info-title" for="exampleInputEmail1">Produit<span>*</span></label>
        {{ form_widget(form.produit_if, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="form-group">
            <label class="info-title" for="exampleInputEmail1">Promotion<span>*</span></label>
            {{ form_widget(form.promotion_id, {'attr': {'class': 'form-control'}}) }}
        </div>

        <input type="submit" value="Create" class="btn btn-success"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('produitenpromotion_index') }}" class="btn btn-default">Back to the list</a>
        </li>
    </ul>

    </div>
</div><!-- mainpanel -->
    {% endblock %}



{% endblock %}
<script type="text/javascript">
    $( "#promotion" ).toggleClass( "nav-parent active" )
</script>
{% include 'AdminFooter.html.twig' %}
